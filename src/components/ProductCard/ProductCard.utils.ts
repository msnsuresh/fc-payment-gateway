import { BadgeTypes } from "../Badge/Badge";

export const BadgeTypesMapper: { [x: string]: BadgeTypes } = {
  hot: BadgeTypes.HOT,
  spicy: BadgeTypes.SPICY,
  new: BadgeTypes.NEW,
  clone: BadgeTypes.CLONE,
  weird: BadgeTypes.WEIRD,
};
